import 'package:flutter/material.dart';

class ManagementDevice extends StatelessWidget {
  const ManagementDevice({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}